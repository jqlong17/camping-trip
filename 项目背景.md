# 露营之旅 — 项目背景（给后续 Agent）

> 持续更新。仓库：`/Users/ruska/projects/3ds/linjian`  
> **设计权威文档**：[docs/游戏设计-SPEC.md](./docs/游戏设计-SPEC.md)（含 **§11 Suno 提示词**、**§12 DEV-XXX 开发日志**）  
> **动画/仪式**：[docs/动画与交互-SPEC.md](./docs/动画与交互-SPEC.md)  
> **Agent Skill**：`.cursor/skills/linjian-camping-3ds/`（亦在 `~/.cursor/skills/linjian-camping-3ds/`）  
> Agent 接手前：先读 SPEC **§12 最新编号** + 动画 SPEC，再读本文件硬件坑。

## 一句话

爱好向 Nintendo 3DS 休闲露营游戏 **「露营之旅」**（工程名 `linjian` / 曾用「林间一夜」）。LovePotion 3.0.2；不上架。完整体验 = 标题页 → 一日露营（光线变化）→ 夜里点灯 → 次日回家 → 周末循环。

## 命名

| 用途 | 名称 |
|------|------|
| 玩家可见标题 | **露营之旅** |
| 仓库 / SD 文件夹 | `linjian`（暂不强制改名，避免打乱已有卡结构） |
| conf identity | `linjian`（`game/conf.lua`） |

窗口标题 / 标题画面文案应逐步改为「露营之旅」。

## 主人与硬件

- 真机：白色 **Old 3DS**，CFW（`luma` / `boot9strap` / `gm9`）。  
- SD 常见挂载：`/Volumes/NO NAME`。  
- **FBI** 装 `.cia`；**本游戏跑 `.3dsx`**（Homebrew Launcher），不是 FBI 安装。  
- 音频需 `dspfirm.cdc`（Rosalina → Dump DSP firmware，或装过 DSP1）。

## 技术选型

| 项 | 现状 |
|----|------|
| 引擎 | LovePotion 3.0.2 |
| 源码 | `game/main.lua`、`conf.lua`、`assets/`、`fonts/` |
| 桌面预览 | `love game`（LÖVE 11.x），400×480 叠屏 |
| 真机 | `./scripts/deploy-to-sd.sh` → `3dsx+game/` 热更 + **`linjian.cia`（FBI）** |
| CIA | 爱好向自打包（LovePotion 官方不支持 CIA）；工具在 `vendor/tools/` |

## 设计方向（2026-08-30 起）

用户要求按**相对完整游戏体验**推进，要点已写入 SPEC：

- **标题画面**为进入后第一屏（上屏美观背景 + 标题；下屏开始/继续等）。  
- 设定：上班族休假，林间有小河；咖啡、炉子等；**早→晚光线变化**；夜里**点露营灯**；次日收拾回城。  
- **周末循环**：下次豆子/小目标（如钓鱼）可略有不同。  
- 美术重点：人物、咖啡道具、帐篷细节；可做漂亮标题背景图。  
- 分阶段：P0 标题 → P1 时段光线 → P2 互动仪式 → P3 闭环 → P4 打磨（见 SPEC §8）。

**当前实现水位**：P0 标题 + 营地；**DEV-017** 起 tileset/河岸/道具/角色已加密度（仍低于三角力量终局，可持续迭代）。下一优先 P1 时段光线。

## 真机踩坑与已修复

1. **「无法运行」**：2019 旧 `boot.firm` / `boot.3dsx` 跑不动新 LovePotion → 已升 **Luma 13.4**；旧文件在 `sd:/_backup_boot_2019/`。  
2. **ndsp / dspfirm.cdc not found**：Dump DSP 或装 DSP1 后解决。  
3. **进 HB 菜单**：须**真正打开**「ダウンロードプレイ」再 Rosalina → Switch hb. title；或装 `hblauncher_loader.cia`。  
4. **中文豆腐块（桌面）**：勿用系统 TTC；用 `game/fonts/zh-ui.ttf`。真机用 `newFont("chinese", 14)`。  
5. **Gatekeeper**：`love` 被拦 → 隐私与安全性仍要打开，或 `xattr -dr com.apple.quarantine /Applications/love.app`。

## 美术与资源

- `game/assets/`：玩家、地块、帐篷、背包图标（持续迭代）。  
- `docs/characters/`、`docs/mockups/`：设定与概念参考。  
- 改完预览：`love game`；再同步 `dist/` 与 SD。

## 仓库结构

```
linjian/
  README.md                 # 项目介绍 + 文档索引
  项目背景.md               # 本文件
  docs/游戏设计-SPEC.md     # 完整体验规格
  game/                     # 可运行内容
  dist/3ds/linjian/         # SD 镜像
  vendor/                   # LovePotion
```

## 给下一个 Agent

1. 以 **[游戏设计-SPEC.md](./docs/游戏设计-SPEC.md)** 为准；**§12 开发日志**用 `DEV-XXX` 续写，勿打乱编号。  
2. 下一功能优先多为 **DEV-012 / P1 时段光线**；音乐生成按 **§11**，接入为 **DEV-015**。  
3. 玩家文案用「露营之旅」；目录名 `linjian` 可暂留。  
4. 新增中文 → 扩展 `zh-ui.ttf` 子集。  
5. 同步：`./scripts/deploy-to-sd.sh`（默认含 CIA）；热更用 `--no-cia`；拔卡前关机。  
6. CIA 为爱好向自打包，改内容后需重打并 FBI 重装；或继续用 3dsx+旁路 `game/`。  
7. 用户偏好 **中文** 沟通。

## 外链

- LovePotion：https://github.com/lovebrew/lovepotion  
- 系统字体：https://lovebrew.github.io/compatibility/system-fonts  
- Luma3DS：https://github.com/LumaTeam/Luma3DS  
